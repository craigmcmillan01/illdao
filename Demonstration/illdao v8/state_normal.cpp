#include "state_normal.h"

using namespace irr::core;

void Normal::execute(Player* owner, float deltaTime)
{
	//owner->getNode()->setFrameLoop(146, 158);
	//vector3df pos = owner->getParticle()->position;
	//owner->getNode()->setPosition(pos);	
}